#include <bits/stdc++.h>
using namespace std;
#define endl "\n";
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int t;
    cin>>t;
    while(t--){
        int n1,n2;
        vector<int> v1,v2;
        for (int i = 0; i < n1; i++)
        {
            cin>>v1[i];
        }
        for (int i = 0; i < n2; i++)
        {
            cin>>v2[i];
        }
        sort(v1.begin(),v1.end());
        sort(v2.begin(),v2.end());
        for (int i = 0; i < max(); i++)
        {
            /* code */
        }
        
        
    }
    
    return 0;
}